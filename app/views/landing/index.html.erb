
<div class="col-md-4">
    <div class="card" style="width: 18rem;">
    <img src="..." class="card-img-left" alt="...">
        <div class="card-body">
            <h5 class="card-title">Welcome to Sample Share</h5>
            <h6 class="card-subtitle">Share your Sound and Find Inspiration</h6><br>
            <% if !user_signed_in? %>
                <%= link_to 'Sign Up', new_user_registration_path, method: :get, class:"btn btn-primary" %>
            <% end %>
        </div>
    </div>
</div>

